_schema-version: "3.1"
ID: capfullstack
description: A simple CAP project.
version: 1.0.0
modules:
  - name: capfullstack-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: capfullstack-db
      - name: capfullstack-auth
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
  - name: capfullstack-db-deployer
    type: hdb
    path: db
    requires:
      - name: capfullstack-db
    parameters:
      buildpack: nodejs_buildpack
  - name: capfullstack
    type: approuter.nodejs
    path: app/
    requires:
      - name: srv-api
        group: destinations
        properties:
          forwardAuthToken: true
          name: srv-api
          url: ~{srv-url}
      - name: capfullstack-auth
      - name: undefined-destination-service
        group: destinations
        properties:
          forwardAuthToken: false
          name: ui5
          url: https://ui5.sap.com
    parameters:
      disk-quota: 256M
      keep-existing-routes: true
      memory: 256M
  - name: undefined-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: undefined-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - capfullstack.zip
          name: capfullstack
          target-path: resources/
  - name: capfullstack1
    type: html5
    path: app/capfullstack
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms: []
resources:
  - name: capfullstack-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: capfullstack-auth
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        tenant-mode: dedicated
        xsappname: capfullstack-${org}-${space}
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
  - name: undefined-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: undefined-html5-srv
      service-plan: app-host
  - name: undefined-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: undefined-destination-service
      service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npx -p @sap/cds-dk cds build --production
